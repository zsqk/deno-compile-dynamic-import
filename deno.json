{
  "tasks": {
    "build": "deno compile -A --output dist/main --include pages main.ts",
    "build:linux": "deno compile -A --output dist/main --include pages --target x86_64-unknown-linux-gnu main.ts",
    "dev": "deno run -A --watch main.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
